<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:background="@drawable/welcombg"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.fycz.dialectorange.StartActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_marginTop="40dp"
        android:layout_alignParentTop="true"
        android:src="@drawable/orange"
        android:id="@+id/welcome_icon"/>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:src="@drawable/welcomtext"
        android:text="@string/app_name_chn"
        android:id="@+id/welcome_text"
        android:layout_below="@+id/welcome_icon"/>

    <ImageView
        android:layout_width="match_parent"
        android:layout_marginTop="50dp"
        android:layout_height="50dp"
        android:src="@drawable/touxiang"
        android:layout_above="@+id/welcome_button"
        android:id="@+id/welcome_touxiang" />

    <ImageButton
        android:layout_width="match_parent"
        android:layout_marginBottom="40dp"
        android:layout_marginTop="30dp"
        android:id="@+id/welcome_button"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:background="@drawable/welcome_botton"
        android:onClick="onClickWelcomeButton"/>

</RelativeLayout>
